<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/stylesheets/style.css">
  <title><%= title %></title>
</head>
<body>
  <nav>
    <% if (user) { %>
      <!-- the active class is for CSS styling. Get back and style later   -->
      <a href="/breweries" <%- title === 'All Breweries' ? 'class="active"' : '' %>>ALL BREWERIES</a>  
      &nbsp;|&nbsp;  
      <a href="/breweries/new" <%- title === 'Add Brewery' ? 'class="active"' : '' %>>ADD BREWERY</a>
      &nbsp;|&nbsp;  
      <a href="/logout">LOG OUT</a>
      &nbsp;|&nbsp;  
      <span><%= user.name %></span>
      &nbsp;
      <img src="<%= user.avatar %>" id="user-avatar" referrerpolicy="no-referrer">
       <% } else { %>
      <a href="/auth/google" class="login">LOG IN&nbsp;<img src="https://i.imgur.com/FHjYyi0.png"></a>
    <% } %>
  </nav>
  <h2><%= title %></h2>